---
// Subtle noise texture for material depth
---

<div class="background-effect" aria-hidden="true">
  <div class="bg-noise"></div>
  <div class="bg-vignette"></div>
</div>

<style>
  .background-effect {
    position: fixed;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    overflow: hidden;
  }

  /* SVG noise texture for paper/material feel */
  .bg-noise {
    position: absolute;
    inset: 0;
    opacity: 0.03;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
  }

  /* Subtle vignette for depth */
  .bg-vignette {
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse at center, transparent 40%, var(--color-bg) 100%);
    opacity: 0.4;
  }

  html.light .bg-noise {
    opacity: 0.015;
  }

  html.light .bg-vignette {
    opacity: 0.2;
  }
</style>
